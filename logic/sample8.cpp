#include <iostream>
using namespace std;

int main(){
   int a = 12, b = 25;
   cout << "a & b = " << (a & b) << endl; 
   cout << "a | b = " << (a | b) << endl; 
   cout << "a ^ b = " << (a ^ b); 

return 0;
}